#include<stdio.h>

int mineatingvalue(int bunches[],int n,int h){
    for(int i=1;i<n;i++){
        if(bunches[i]>right){

        }
        int answer=right;
        while (left<=right)
        {
            int mid=left+
        }
    }
}